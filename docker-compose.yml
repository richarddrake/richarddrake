# 这个 Compose 文件负责一键拉起 Vue 前端、FastAPI 后端和 MySQL 的可复现演示环境。
name: ai-testcase-platform

services:
  mysql:
    image: mysql:8.0
    container_name: ai-test-mysql
    command:
      - --default-authentication-plugin=mysql_native_password
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: ${DOCKER_MYSQL_ROOT_PASSWORD:-ai_test_root}
      MYSQL_DATABASE: ${DOCKER_MYSQL_DATABASE:-ai_testcase}
      MYSQL_USER: ${DOCKER_MYSQL_USER:-ai_test_user}
      MYSQL_PASSWORD: ${DOCKER_MYSQL_PASSWORD:-ai_test_password}
    ports:
      - "${DOCKER_MYSQL_PORT:-3307}:3306"
    volumes:
      - ai-test-mysql-data:/var/lib/mysql
      - ./docker/mysql/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u$${MYSQL_USER} -p$${MYSQL_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  redis:
    image: redis:7.4-alpine
    container_name: ai-test-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "${DOCKER_REDIS_PORT:-6379}:6379"
    volumes:
      - ai-test-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: ai-test-backend
    environment:
      DATABASE_ENABLED: "true"
      MYSQL_HOST: mysql
      MYSQL_PORT: "3306"
      MYSQL_USER: ${DOCKER_MYSQL_USER:-ai_test_user}
      MYSQL_PASSWORD: ${DOCKER_MYSQL_PASSWORD:-ai_test_password}
      MYSQL_DATABASE: ${DOCKER_MYSQL_DATABASE:-ai_testcase}
      MYSQL_CHARSET: utf8mb4
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      LLM_TIMEOUT_SECONDS: ${LLM_TIMEOUT_SECONDS:-120}
      LLM_MAX_TOKENS: ${LLM_MAX_TOKENS:-5000}
      API_DOC_LLM_ENHANCE: ${API_DOC_LLM_ENHANCE:-true}
      API_DOC_DEFAULT_BASE_URL: ${API_DOC_DEFAULT_BASE_URL:-http://127.0.0.1:8080}
      API_RUNNER_ALLOW_LOCALHOST: "true"
      AUTH_SECRET_KEY: ${AUTH_SECRET_KEY:-ai-testcase-docker-demo-secret}
      AUTH_TOKEN_EXPIRE_MINUTES: ${AUTH_TOKEN_EXPIRE_MINUTES:-1440}
      AUTH_COOKIE_SECURE: ${AUTH_COOKIE_SECURE:-false}
      APP_ADMIN_USERNAME: ${APP_ADMIN_USERNAME:-admin}
      APP_ADMIN_PASSWORD: ${APP_ADMIN_PASSWORD:-Admin@123456}
      APP_ADMIN_DISPLAY_NAME: ${APP_ADMIN_DISPLAY_NAME:-管理员}
      REDIS_ENABLED: ${REDIS_ENABLED:-true}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      REDIS_KEY_PREFIX: ${REDIS_KEY_PREFIX:-ai-test}
      TASK_QUEUE_ENABLED: ${TASK_QUEUE_ENABLED:-true}
      TASK_WORKER_CONCURRENCY: ${TASK_WORKER_CONCURRENCY:-2}
      TASK_SUBMIT_RATE_LIMIT: ${TASK_SUBMIT_RATE_LIMIT:-30}
      TASK_SUBMIT_RATE_WINDOW_SECONDS: ${TASK_SUBMIT_RATE_WINDOW_SECONDS:-60}
      TASK_RESULT_TTL_SECONDS: ${TASK_RESULT_TTL_SECONDS:-86400}
    ports:
      - "${DOCKER_BACKEND_PORT:-8000}:8000"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ""
    container_name: ai-test-frontend
    ports:
      - "${DOCKER_FRONTEND_PORT:-5173}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  ai-test-mysql-data:
  ai-test-redis-data:
